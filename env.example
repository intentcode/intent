# =============================================
# GITHUB APP (recommended - read-only permissions)
# =============================================
# Create a GitHub App at: https://github.com/settings/apps/new
# Required permissions: Contents (read), Pull requests (read), Metadata (read)
# Set User authorization callback URL to: http://localhost:3001/api/auth/callback (local)

GITHUB_APP_ID=your_app_id
GITHUB_APP_CLIENT_ID=your_app_client_id
GITHUB_APP_CLIENT_SECRET=your_app_client_secret

# Private key - choose ONE option:
# Option 1 (local): Path to the .pem file downloaded from GitHub App settings
GITHUB_APP_PRIVATE_KEY_PATH=/path/to/your-app.private-key.pem

# Option 2 (production/Vercel): PEM content with newlines replaced by \n
# To convert: cat key.pem | awk '{printf "%s\\n", $0}'
# GITHUB_APP_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----\nMIIE...\n-----END RSA PRIVATE KEY-----

# =============================================
# LEGACY OAUTH APP (fallback if GitHub App not configured)
# =============================================
# Create an OAuth app at: https://github.com/settings/developers
# Set callback URL to: http://localhost:3001/api/auth/callback (local)
GITHUB_CLIENT_ID=your_client_id
GITHUB_CLIENT_SECRET=your_client_secret

# JWT secret for session tokens (generate with: openssl rand -hex 32)
JWT_SECRET=your-secret-key-change-in-production

# Optional: GitHub Personal Access Token (increases API rate limit from 60 to 5000 req/hour)
GITHUB_TOKEN=ghp_xxxxxxxxxxxx

# Optional: Default GitHub repo to show on landing page
# Format: owner/repo (e.g., intentcode/intent)
DEFAULT_REPO=intentcode/intent

# Optional: Default local repo path for /local mode
# This repo will be loaded automatically when visiting /local
DEFAULT_REPO_PATH=/Users/me/projects/intent
